# @zjf-harness/cli

Run `pnpm --filter @zjf-harness/cli start` in a terminal to open the native
full-screen TUI. A positional prompt starts the first turn immediately.

`--mode plan | accept-edits | bypass` selects the initial mode (default
`plan`). `-p` / `--print` stays non-interactive and does not change mode.
